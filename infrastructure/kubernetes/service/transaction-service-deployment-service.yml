apiVersion: v1
kind: Service
metadata:
  name: wali-transaction-service
  labels:
    app: wali-transaction-service
spec:
  clusterIP: None
  selector:
    app: wali-transaction-service
  ports:
      port: 8080
      targetPort: 8080