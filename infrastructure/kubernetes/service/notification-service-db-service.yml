apiVersion: v1
kind: Service
metadata:
  name: wali-notification-service-db
  labels:
    app: wali-notification-service-db
spec:
  clusterIP: None
  selector:
    app: wali-notification-service-db
  ports:
      port: 5432
      targetPort: 5432