apiVersion: v1
kind: Service
metadata:
  name: wali-auth-service
  labels:
    app: wali-auth-service
spec:
  clusterIP: None
  selector:
    app: wali-auth-service
  ports:
      port: 8080
      targetPort: 8080