apiVersion: v1
kind: Service
metadata:
  name: wali-notification-service
  labels:
    app: wali-notification-service
spec:
  selector:
    app: wali-notification-service
  ports:
      - port: 8080
        targetPort: 8080