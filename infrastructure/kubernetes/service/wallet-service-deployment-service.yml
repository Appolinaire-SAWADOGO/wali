apiVersion: v1
kind: Service
metadata:
  name: wali-wallet-service
  labels:
    app: wali-wallet-service
spec:
  clusterIP: None
  selector:
    app: wali-wallet-service
  ports:
      port: 8080
      targetPort: 8080