apiVersion: apps/v1
kind: Deployment
metadata:
  name: wali-auth-service
  labels:
    app: wali-auth-service
spec:
  replicas: 3
  selector:
    matchLabels:
      app: wali-auth-service
  template:
    metadata:
      labels:
        app: wali-auth-service
    spec:
      containers:
        - name: wali-auth-service
          image: 08072005/wali-auth-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          env:
            - name: DB_URL
              value: "jdbc:postgresql://wali-auth-service-db.default.svc.cluster.local:5432/wali-auth-service-db"
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: wali-auth-service-db-password
                  key: DB_PASSWORD
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: wali-jwt-secret
                  key: JWT_SECRET
            - name: KAFKA_BOOTSTRAP_SERVERS
              value: wali-kafka.default.svc.cluster.local:9092
            - name: DB_USER
              value: wali-auth-service-db-user