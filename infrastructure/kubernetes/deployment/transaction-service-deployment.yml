apiVersion: apps/v1
kind: Deployment
metadata:
  name: wali-transaction-service
  labels:
    app: wali-transaction-service
spec:
  replicas: 3
  selector:
    matchLabels:
      app: wali-transaction-service
  template:
    metadata:
      labels:
        app: wali-transaction-service
    spec:
      containers:
        - name: wali-transaction-service
          image: 08072005/wali-transaction-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          env:
            - name: DB_URL
              value: "jdbc:postgresql://wali-transaction-service-db.default.svc.cluster.local:5432/wali-transaction-service-db"
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: wali-transaction-service-db-password
                  key: DB_PASSWORD
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: wali-jwt-secret
                  key: JWT_SECRET
            - name: KAFKA_BOOTSTRAP_SERVERS
              value: wali-kafka.default.svc.cluster.local:9092
            - name: DB_USER
              value: wali-transaction-service-db-user